<div class="panel-group" id="{{ accordion.id }}" role="tablist" aria-multiselectable="true">
  {% for item in accordion.items %}
  <div class="panel panel-default">
    <div class="panel-heading" role="tab" id="{{ item.heading_id }}">
      <h4 class="panel-title">
        <a role="button" data-toggle="collapse" data-parent="#{{ accordion.id }}" href="#{{ item.body_id }}" aria-expanded="true" aria-controls="{{ item.body_id }}">
          {{ item.title }}
        </a>
      </h4>
    </div>
    <div id="{{ item.body_id }}" class="panel-collapse collapse{% if loop.first %} in{% endif %}" role="tabpanel" aria-labelledby="{{ heading_id }}">
      <div class="panel-body">
        {{ accordion.sections[item.section] }}
      </div>
    </div>
  </div>
  {% endfor %}
</div>