<div {% include 'partials/_attribute.html.twig' with {'attribute_name': 'id', 'attribute_value': id} %} class="jumbotron {{ attributes['class'] }}" {% include 'partials/_attributes.html.twig' with {'ignore': ['class']} %} {% if(image is defined and image != null) %} style="background-image: url('{{ grav.page.media.images[image].url }}')"{% endif %}>
    {% if fullwidth == true %}
    <div class="container">
    {{ content }}
    </div>
    {% else %}
    {{ content }}
    {% endif %}
</div>
